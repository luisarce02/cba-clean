spring:
  application:
    name: incident-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/cbaclean}
      database: ${MONGO_DATABASE:cbaclean}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health
